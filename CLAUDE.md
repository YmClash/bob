# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository Overview

This is a newly initialized repository called "bob" with minimal structure. The repository is currently in its initial setup phase with only the following elements:

- MIT License file
- Empty `akili/` directory
- `.idea/` directory (WebStorm/IntelliJ IDE configuration)
- Git repository initialized on branch `akili_local`

## Development Status

This appears to be a brand new project with no code implementation yet. The repository structure suggests it may become a WebStorm/IntelliJ-based project given the presence of `.idea/` configuration files.

## Next Steps

When implementing features in this repository:
1. Determine the project's programming language and framework
2. Set up appropriate build tooling and dependencies
3. Establish project structure within the `akili/` directory or at the root level
4. Add README.md with project description and setup instructions